- p:
    attributes:
      class: some class
      id: an-id
    content: |

      This is a paragraph of text starting with a newline
      that is set to preserve newlines in the originating YAML file.

- p:
    content: This tag has content but no attributes.
- input:
    attributes:
      value: This tag has an attribute but no content.
